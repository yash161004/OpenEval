from typing import Literal

def format_report(results: dict, format: Literal["markdown", "json"]) -> str:
    """
    Formats the evaluation results into the specified format.
    """
    raise NotImplementedError()
